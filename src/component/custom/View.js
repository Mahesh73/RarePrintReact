import React from 'react'

const View = (props) => {
    return (
        <>
            <div className="view card">
                <h1>{props.fname}</h1>
            </div>
        </>
    )
}

export default View
